"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function getWindowedListBoundaries({ buffer = 5, containerHeight, containerScrollPosition, enabled = true, itemHeight, length, }) {
    if (!enabled)
        return { end: length - 1, start: 0 };
    if (containerScrollPosition === undefined || containerHeight === undefined)
        return { end: 0, start: 0 };
    const top = Math.floor(containerScrollPosition / itemHeight);
    const bottom = Math.ceil((containerHeight + containerScrollPosition) / itemHeight);
    return {
        end: bottom + buffer > length - 1 ? length - 1 : bottom + buffer,
        start: top - buffer < 0 ? 0 : top - buffer,
    };
}
exports.getWindowedListBoundaries = getWindowedListBoundaries;
//# sourceMappingURL=getWindowedListBoundaries.js.map