"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stringComparator = (stringA, stringB) => {
    const upperCasedStringA = stringA.toUpperCase();
    const upperCasedStringB = stringB.toUpperCase();
    if (upperCasedStringA < upperCasedStringB)
        return -1;
    if (upperCasedStringA > upperCasedStringB)
        return 1;
    return 0;
};
exports.doDefaultActionListSort = (data, columns, id, sortDirection) => {
    const sortedData = [...data];
    const updatedColumns = [...columns];
    const targetColumn = updatedColumns.find((column) => column.id === id);
    columns.forEach((column) => delete column.sortDirection);
    if (targetColumn) {
        if (targetColumn.type === 'number') {
            if (sortDirection === 'desc') {
                sortedData.sort((a, b) => b[id] - a[id]);
            }
            else {
                sortedData.sort((a, b) => a[id] - b[id]);
            }
        }
        else if (targetColumn.type === 'string') {
            if (sortDirection === 'desc') {
                sortedData.sort((a, b) => exports.stringComparator(b[id], a[id]));
            }
            else {
                sortedData.sort((a, b) => exports.stringComparator(a[id], b[id]));
            }
        }
        targetColumn.sortDirection = sortDirection;
    }
    return {
        columns: updatedColumns,
        data: sortedData,
    };
};
//# sourceMappingURL=sort_utils.js.map