"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const padStart_1 = __importDefault(require("lodash/padStart"));
const toString_1 = __importDefault(require("lodash/toString"));
const map_1 = __importDefault(require("lodash/map"));
exports.cycleHourDisplay = (format, hour) => {
    if (format === '12h') {
        if (hour === 0) {
            return 12;
        }
        else if (hour > 12) {
            return hour - 12;
        }
    }
    return hour;
};
exports.formatTimeString = (number) => {
    return padStart_1.default(toString_1.default(number), 2, '0');
};
exports.parseBase10Int = (value) => value.length ? parseInt(value, 10) : 0;
exports.isValidTime = (value) => {
    if (!value) {
        return true;
    }
    const [hour = 0, minute = 0] = map_1.default(value.split(':'), exports.parseBase10Int);
    if (hour < 24 && minute <= 60) {
        return true;
    }
    return false;
};
//# sourceMappingURL=utils.js.map