"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importStar(require("react"));
const Fieldset_1 = require("../../Fieldset");
const Fields_1 = require("../../Fields");
exports.CheckboxGroup = ({ disabled, inline, options, defaultValue = [], value, onChange, }) => {
    const [currentOptions, setCurrentOptions] = react_1.useState(value || defaultValue);
    const handleChange = (option) => {
        const newValues = currentOptions.includes(option)
            ? [...currentOptions.filter((v) => v !== option)]
            : [...currentOptions, option];
        !value && setCurrentOptions(newValues);
        onChange && onChange(newValues);
    };
    const checkboxes = options.map((option, index) => (react_1.default.createElement(Fields_1.FieldCheckbox, { onChange: () => handleChange(option.value), disabled: option.disabled || disabled, key: index, label: option.label, checked: currentOptions.includes(option.value), value: option.value })));
    return (react_1.default.createElement(Fieldset_1.Fieldset, { "data-testid": "checkbox-list", inline: inline }, checkboxes));
};
//# sourceMappingURL=CheckboxGroup.js.map