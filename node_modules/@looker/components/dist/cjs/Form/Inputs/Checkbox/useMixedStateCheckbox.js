"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = require("react");
const some_1 = __importDefault(require("lodash/some"));
const every_1 = __importDefault(require("lodash/every"));
function useMixedStateCheckbox({ parent, children }) {
    react_1.useEffect(() => {
        if (every_1.default(children, ['state', true])) {
            parent.setState(true);
        }
        else if (some_1.default(children, ['state', true])) {
            parent.setState('mixed');
        }
        else {
            parent.setState(false);
        }
    }, [children, parent]);
    const handleParentChange = () => {
        const newState = parent.state !== true;
        parent.setState(newState);
        children.map((child) => child.setState(newState));
    };
    return handleParentChange;
}
exports.useMixedStateCheckbox = useMixedStateCheckbox;
//# sourceMappingURL=useMixedStateCheckbox.js.map