"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importStar(require("react"));
const design_tokens_1 = require("@looker/design-tokens");
const styled_components_1 = __importDefault(require("styled-components"));
const sortBy_1 = __importDefault(require("lodash/sortBy"));
const indexOf_1 = __importDefault(require("lodash/indexOf"));
const startsWith_1 = __importDefault(require("lodash/startsWith"));
const partial_1 = __importDefault(require("lodash/partial"));
const map_1 = __importDefault(require("lodash/map"));
const isEqual_1 = __importDefault(require("lodash/isEqual"));
const utils_1 = require("../../../utils");
const sort = (arr) => arr.sort((a, b) => a - b);
const findClosestIndex = (value, newPoint) => {
    const { index: closestIndex } = sortBy_1.default(value.map((p, i) => ({
        distance: Math.abs(p - newPoint),
        index: i,
    })), 'distance')[0];
    return closestIndex;
};
const createNewValue = (value, newPoint, focusedIndex) => {
    const indexToReplace = focusedIndex === undefined
        ? findClosestIndex(value, newPoint)
        : focusedIndex;
    const newValue = Object.assign([], value, { [indexToReplace]: newPoint });
    return sort(newValue);
};
const roundToStep = (min, max, newPoint, step) => {
    const roundedPoint = Math.round((newPoint - min) / step) * step + min;
    return Math.max(Math.min(roundedPoint, max), min);
};
const calculatePointValue = (mouseX, containerRect, min, max, step) => {
    const mousePosition = mouseX - containerRect.left;
    const possibleValueRange = max - min;
    const newPoint = (mousePosition / containerRect.width) * possibleValueRange + min;
    return roundToStep(min, max, newPoint, step);
};
const boundValueProp = (min, max, value) => {
    return map_1.default(value || [min, max], (point) => {
        const boundedPoint = Math.max(Math.min(point, max), min);
        if (boundedPoint !== point) {
            console.warn(`<RangeSlider />: The value '${point}' falls outside the possible range (MIN: ${min}, MAX: ${max}). Please adjust min and max props accordingly.`);
        }
        return boundedPoint;
    });
};
exports.InternalRangeSlider = ({ className, min = 0, max = 10, step = 1, value: valueProp, defaultValue: defaultValueProp, onChange, disabled = false, readOnly: readOnlyProp = false, 'aria-labelledby': ariaLabelledBy, }) => {
    const unintentionalReadOnly = utils_1.useReadOnlyWarn('RangeSlider', valueProp, onChange);
    const readOnly = readOnlyProp || unintentionalReadOnly;
    const boundedValue = boundValueProp(min, max, valueProp || defaultValueProp);
    const [value, setValue] = react_1.useState(sort(boundedValue));
    const [containerRef, setContainerRef] = react_1.useState(null);
    const [focusedThumb, setFocusedThumb] = react_1.useState();
    const containerRect = utils_1.useMeasuredElement(containerRef);
    const { mousePos, isMouseDown } = utils_1.useMouseDragPosition(containerRef);
    const prevMouseDown = utils_1.usePreviousValue(isMouseDown);
    const minThumbRef = react_1.useRef(null);
    const maxThumbRef = react_1.useRef(null);
    const [minValue, maxValue] = value;
    const minPos = ((minValue - min) / (max - min)) * containerRect.width;
    const maxPos = ((maxValue - min) / (max - min)) * containerRect.width;
    const fillWidth = maxPos - minPos;
    const thumbRefs = [minThumbRef, maxThumbRef];
    const focusChangedPoint = (newValue, newPoint) => {
        const indexToFocus = indexOf_1.default(newValue, newPoint);
        const refToFocus = thumbRefs[indexToFocus];
        requestAnimationFrame(() => {
            refToFocus.current && refToFocus.current.focus();
        });
    };
    const incrementPoint = (point) => {
        return Math.min(point + step, max);
    };
    const decrementPoint = (point) => {
        return Math.max(point - step, min);
    };
    const handleKeyboardNav = (e) => {
        if (!disabled && !readOnly) {
            if (startsWith_1.default(e.key, 'Arrow') && focusedThumb !== undefined) {
                e.preventDefault();
                const unfocusedThumb = focusedThumb === 0 ? 1 : 0;
                const mutationFn = e.key === 'ArrowUp' || e.key === 'ArrowRight'
                    ? incrementPoint
                    : decrementPoint;
                const newPoint = mutationFn(value[focusedThumb]);
                const newValue = sort([newPoint, value[unfocusedThumb]]);
                focusChangedPoint(newValue, newPoint);
                setValue(newValue);
            }
        }
    };
    const focusMinThumb = () => {
        if (!disabled && !readOnly) {
            setFocusedThumb(0);
        }
    };
    const focusMaxThumb = () => {
        if (!disabled && !readOnly) {
            setFocusedThumb(1);
        }
    };
    const handleBlur = () => {
        setFocusedThumb(undefined);
    };
    const handleMouseEvent = (maintainFocus) => {
        if (!disabled && !readOnly) {
            const newPoint = calculatePointValue(mousePos.x, containerRect, min, max, step);
            const newValue = createNewValue(value, newPoint, maintainFocus ? focusedThumb : undefined);
            focusChangedPoint(newValue, newPoint);
            setValue(newValue);
        }
    };
    const handleMouseDown = partial_1.default(handleMouseEvent, false);
    const handleMouseDrag = partial_1.default(handleMouseEvent, true);
    react_1.useEffect(() => {
        if (isMouseDown && prevMouseDown) {
            handleMouseDrag();
        }
    }, [mousePos, isMouseDown]);
    react_1.useEffect(() => {
        const boundedValue = boundValueProp(min, max, valueProp);
        if (!isEqual_1.default(value, boundedValue)) {
            setValue(sort(boundedValue));
        }
    }, [valueProp]);
    react_1.useEffect(() => {
        if (!isEqual_1.default(value, valueProp)) {
            onChange && onChange(value);
        }
    }, [value]);
    return (react_1.default.createElement("div", { "data-testid": "range-slider-wrapper", onMouseDown: handleMouseDown, className: className, ref: setContainerRef },
        react_1.default.createElement(SliderTrack, null,
            react_1.default.createElement(SliderFill, { fillStart: minPos, fillWidth: fillWidth, disabled: disabled }),
            react_1.default.createElement(ThumbLabel, { position: minPos, focus: focusedThumb === 0, disabled: disabled }, minValue),
            react_1.default.createElement(ThumbLabel, { position: maxPos, focus: focusedThumb === 1, disabled: disabled }, maxValue),
            react_1.default.createElement(Thumb, { position: minPos, tabIndex: (disabled ? '-1' : '0'), onFocus: focusMinThumb, onBlur: handleBlur, onKeyDown: handleKeyboardNav, ref: minThumbRef, disabled: disabled, "aria-label": "Minimum Value", role: "slider", "aria-valuemin": min, "aria-valuemax": max, "aria-disabled": disabled, "aria-valuenow": value[0], "aria-labelledBy": ariaLabelledBy }),
            react_1.default.createElement(Thumb, { position: maxPos, tabIndex: (disabled ? '-1' : '0'), onFocus: focusMaxThumb, onBlur: handleBlur, onKeyDown: handleKeyboardNav, ref: maxThumbRef, disabled: disabled, "aria-label": "Maximum Value", role: "slider", "aria-valuemin": min, "aria-valuemax": max, "aria-disabled": disabled, "aria-valuenow": value[1], "aria-labelledby": ariaLabelledBy }))));
};
exports.RangeSlider = styled_components_1.default(exports.InternalRangeSlider).withConfig({ displayName: "RangeSlider", componentId: "sc-aiqru1" }) `
  ${design_tokens_1.reset}
  ${design_tokens_1.space}
  padding: ${({ theme: { space } }) => `${space.xlarge} 0 ${space.small}`};
  max-width: 500px;
`;
const SliderTrack = styled_components_1.default.div.withConfig({ displayName: "SliderTrack", componentId: "sc-1ltg0tt" }) `
  height: 4px;
  background: ${({ theme }) => theme.colors.palette.charcoal200};
  border-radius: 2px;
  position: relative;
`;
const ThumbLabel = styled_components_1.default.div.withConfig({ displayName: "ThumbLabel", componentId: "sc-z0gnc1" }) `
  user-select: none;
  position: absolute;
  top: -30px;
  transform: translateX(calc(${({ position = 0 }) => `${position}px`} - 50%));
  text-align: center;
  color: ${({ theme: { colors }, disabled }) => disabled ? colors.palette.charcoal700 : colors.semanticColors.primary.main};
  padding: 0 0.5rem;
  border-radius: 1rem;
  z-index: ${({ focus }) => (focus ? 1 : 0)};
  background: ${({ theme, focus }) => focus ? theme.colors.palette.purple100 : `rgba(255, 255, 255, 0.8)`};
`;
const Thumb = styled_components_1.default.div.withConfig({ displayName: "Thumb", componentId: "sc-1m2uy74" }) `
  border-radius: 100%;
  cursor: pointer;
  border: 3px solid
    ${({ theme: { colors }, disabled }) => disabled
    ? colors.palette.charcoal500
    : colors.semanticColors.primary.main};
  height: 16px;
  width: 16px;
  background: ${({ theme }) => theme.colors.palette.white};
  position: absolute;
  top: -6px;
  margin-left: -8px;
  transform: translateX(${({ position = 0 }) => `${position}px`});
  &:focus {
    outline: none;
    border-width: ${({ disabled }) => (disabled ? '3px' : '5px')};
    z-index: 1;
  }
`;
const SliderFill = styled_components_1.default.div.withConfig({ displayName: "SliderFill", componentId: "sc-1dygz3g" }) `
  height: 100%;
  background: ${({ theme: { colors }, disabled }) => disabled ? colors.palette.charcoal400 : colors.semanticColors.primary.main};
  position: absolute;
  left: ${({ fillStart }) => fillStart}px;
  width: ${({ fillWidth }) => fillWidth}px;
`;
//# sourceMappingURL=RangeSlider.js.map