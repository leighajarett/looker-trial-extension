"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = require("react");
const state_1 = require("../utils/state");
const visibleStates = [
    state_1.ComboboxState.SUGGESTING,
    state_1.ComboboxState.NAVIGATING,
    state_1.ComboboxState.INTERACTING,
];
exports.getIsVisible = (state) => visibleStates.includes(state);
function useComboboxToggle(state, option, onOpen, onClose) {
    const isVisible = exports.getIsVisible(state);
    const isVisibleRef = react_1.useRef(isVisible);
    react_1.useEffect(() => {
        if (isVisible && !isVisibleRef.current) {
            onOpen && onOpen(option);
            isVisibleRef.current = true;
        }
        else if (!isVisible && isVisibleRef.current) {
            onClose && onClose(option);
            isVisibleRef.current = false;
        }
    }, [isVisible, isVisibleRef, onOpen, onClose, option]);
    return isVisible;
}
exports.useComboboxToggle = useComboboxToggle;
//# sourceMappingURL=useComboboxToggle.js.map