"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = require("react");
const utils_1 = require("../../../../utils");
function useComboboxRefs(forwardedRef) {
    const [wrapperElement, ref] = utils_1.useCallbackRef(forwardedRef);
    const optionsRef = react_1.useRef([]);
    const popoverRef = react_1.useRef(null);
    const autoCompletePropRef = react_1.useRef(true);
    const readOnlyPropRef = react_1.useRef(false);
    const persistSelectionPropRef = react_1.useRef(false);
    const closeOnSelectPropRef = react_1.useRef(true);
    const windowedOptionsPropRef = react_1.useRef(false);
    const isAutoScrollingRef = react_1.useRef(false);
    return {
        autoCompletePropRef,
        closeOnSelectPropRef,
        isAutoScrollingRef,
        optionsRef,
        persistSelectionPropRef,
        popoverRef,
        readOnlyPropRef,
        ref,
        windowedOptionsPropRef,
        wrapperElement,
    };
}
exports.useComboboxRefs = useComboboxRefs;
//# sourceMappingURL=useComboboxRefs.js.map