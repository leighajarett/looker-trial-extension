"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.cartesian2polar = (coord) => ({
    angle: Math.atan2(coord.y, coord.x),
    radius: Math.round(Math.sqrt(coord.x * coord.x + coord.y * coord.y)),
});
exports.polar2cartesian = (coord) => ({
    x: coord.radius * Math.cos(coord.angle),
    y: coord.radius * Math.sin(coord.angle),
});
exports.rad2deg = (rad) => (rad / (2 * Math.PI)) * 360;
exports.deg2rad = (angle) => angle * (Math.PI / 180);
exports.diameter = (radius) => 2 * radius;
exports.translate = (by, val) => val + by;
exports.translateDiagonal = (by, coordinate) => ({
    x: exports.translate(by, coordinate.x),
    y: exports.translate(by, coordinate.y),
});
exports.scaleRadius = (by, coord) => (Object.assign(Object.assign({}, coord), { radius: coord.radius * by }));
exports.limitByRadius = (coord, radius) => {
    const { x, y } = exports.translateDiagonal(-radius, coord);
    const distanceFromCenter = Math.min(Math.sqrt(x * x + y * y), radius);
    const angle = Math.atan2(y, x);
    const newCoord = exports.polar2cartesian({ angle, radius: distanceFromCenter });
    return {
        x: newCoord.x + radius,
        y: newCoord.y + radius,
    };
};
//# sourceMappingURL=math_utils.js.map