var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { cloneElement, Children, useContext, useCallback, useState, } from 'react';
import { useTooltip } from '../Tooltip';
import { MenuContext } from './MenuContext';
function wrapCallback(cbParent, cbChild) {
    return (e) => {
        cbParent(e);
        cbChild && cbChild(e);
    };
}
export const MenuDisclosure = ({ children, tooltip, tooltipPlacement, }) => {
    const { disabled, id, showDisclosure, isOpen, setOpen, triggerElement, triggerCallbackRef, } = useContext(MenuContext);
    const _a = useTooltip({
        content: tooltip,
        disabled: isOpen,
        placement: tooltipPlacement || 'top',
        triggerElement,
    }), _b = _a.eventHandlers, { onFocus, onBlur } = _b, eventHandlers = __rest(_b, ["onFocus", "onBlur"]), { tooltip: renderedTooltip } = _a;
    const [focused, setFocused] = useState(false);
    function handleFocus() {
        setFocused(true);
        onFocus();
    }
    function handleBlur() {
        setFocused(false);
        onBlur();
    }
    const handleClick = useCallback(() => {
        setOpen && setOpen(!isOpen);
    }, [setOpen, isOpen]);
    if (!showDisclosure && !isOpen && !focused)
        return null;
    const allCallbacks = Object.assign(Object.assign({}, (tooltip ? eventHandlers : {})), { onBlur: handleBlur, onClick: handleClick, onFocus: handleFocus });
    const cloned = Children.map(children, (child) => {
        const childProps = child.props;
        const wrappedCallbacks = {};
        Object.keys(allCallbacks).forEach((cbName) => {
            const cbParent = Reflect.get(allCallbacks, cbName);
            wrappedCallbacks[cbName] = wrapCallback(cbParent, childProps[cbName]);
        });
        return cloneElement(child, Object.assign(Object.assign({}, wrappedCallbacks), { 'aria-controls': id, 'aria-expanded': isOpen, 'aria-haspopup': true, className: `${childProps.className || ''}${isOpen ? ' active' : ''}`, disabled, id: `button-${id}`, ref: triggerCallbackRef }));
    });
    return (React.createElement(React.Fragment, null,
        renderedTooltip,
        cloned));
};
//# sourceMappingURL=MenuDisclosure.js.map