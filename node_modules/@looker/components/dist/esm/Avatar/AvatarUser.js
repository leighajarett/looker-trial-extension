import React from 'react';
import styled from 'styled-components';
import { avatarCSS } from './Avatar';
const AvatarLayout = ({ color, className, user, size, }) => {
    const firstInitial = user && user.first_name && user.first_name[0];
    const lastInitial = user && user.last_name && user.last_name[0];
    return (React.createElement("div", { className: className },
        React.createElement(AvatarInitials, { color: color }, size === 'xxsmall'
            ? `${firstInitial}`
            : `${firstInitial}${lastInitial}`),
        user && user.avatar_url && (React.createElement(AvatarPhoto, { color: color, type: "image/png", data: user.avatar_url }))));
};
const AvatarPhoto = styled.object.withConfig({ displayName: "AvatarPhoto", componentId: "sc-19nfdnw" }) `
  width: 100%;
  height: 100%;
  position: absolute;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
`;
const AvatarInitials = styled.div.attrs((props) => ({
    bg: props.color,
})).withConfig({ displayName: "AvatarInitials", componentId: "sc-1o042m8" }) `
  color: ${(props) => props.theme.colors.palette.white};
`;
export const AvatarUser = styled(AvatarLayout).attrs((props) => ({
    size: props.size || 'small',
})).withConfig({ displayName: "AvatarUser", componentId: "sc-wozvuf" }) `
  ${avatarCSS}

  background: currentColor;
  position: relative;
`;
AvatarUser.defaultProps = {
    color: 'palette.purple400',
    size: 'small',
};
//# sourceMappingURL=AvatarUser.js.map