import React from 'react';
import styled from 'styled-components';
import { variant } from 'styled-system';
import { Icon } from '../Icon';
import { avatarCSS } from './Avatar';
const size = variant({
    prop: 'size',
    variants: {
        xxsmall: {
            height: '16px',
            width: '16px',
        },
        xsmall: {
            height: '20px',
            width: '20px',
        },
        small: {
            height: '20px',
            width: '20px',
        },
        medium: {
            height: '30px',
            width: '30px',
        },
        large: {
            height: '36px',
            width: '36px',
        },
    },
});
const AvatarLayout = ({ className, color, icon = 'User', }) => (React.createElement("div", { className: className },
    React.createElement(Icon, { name: icon, color: color })));
export const AvatarIcon = styled(AvatarLayout).withConfig({ displayName: "AvatarIcon", componentId: "sc-17rvwdz" }) `
  ${avatarCSS}
  border: solid 1px currentColor;

  ${Icon} {
    ${size}
  }
`;
AvatarIcon.defaultProps = {
    bg: 'palette.white',
    color: 'palette.purple300',
    size: 'small',
};
//# sourceMappingURL=AvatarIcon.js.map