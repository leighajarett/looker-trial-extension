import React from 'react';
import styled from 'styled-components';
import { Flex } from '../Layout';
import { IconButton } from '../Button';
import { Text } from '../Text';
const PaginationButton = (props) => (React.createElement(IconButton, Object.assign({ outline: true, mx: "xxsmall" }, props)));
const PaginationLayout = ({ className, current, pages, onChange, }) => {
    if (pages <= 1)
        return null;
    const first = () => onChange(1);
    const previous = () => onChange(current - 1);
    const next = () => onChange(current + 1);
    const last = () => onChange(pages);
    return (React.createElement(Flex, { alignItems: "center", className: className },
        React.createElement(PaginationButton, { label: "First page of results", icon: "DoubleChevronLeft", onClick: first, disabled: current <= 2 }),
        React.createElement(PaginationButton, { label: "Previous page of results", icon: "CaretLeft", onClick: previous, disabled: current === 1 }),
        React.createElement(Text, { fontSize: "small", mx: "xxsmall" },
            React.createElement("b", null, current),
            " of ",
            pages),
        React.createElement(PaginationButton, { label: "Next page of results", icon: "CaretRight", onClick: next, disabled: pages - current === 0 }),
        React.createElement(PaginationButton, { mr: "none", label: "Last page of results", icon: "DoubleChevronRight", onClick: last, disabled: pages - current <= 1 })));
};
export const Pagination = styled(PaginationLayout).withConfig({ displayName: "Pagination", componentId: "sc-18mb581" }) ``;
//# sourceMappingURL=Pagination.js.map