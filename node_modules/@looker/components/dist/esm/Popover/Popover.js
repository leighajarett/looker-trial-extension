var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useEffect, useMemo, useState, } from 'react';
import { Box } from '../Layout';
import { ModalPortal, ModalContext } from '../Modal';
import { OverlaySurface } from '../Overlay/OverlaySurface';
import { useCallbackRef, useControlWarn, useFocusTrap, useHovered, usePopper, useScrollLock, useForkedRef, } from '../utils';
function useVerticalSpace(element, pin, placement, isOpen) {
    const [spaceTop, setSpaceTop] = useState(0);
    const [spaceBottom, setSpaceBottom] = useState(0);
    const placementIsBottom = placement && placement.includes('bottom');
    const placementIsTop = placement && placement.includes('top');
    useEffect(() => {
        function getVerticalSpace() {
            if (element) {
                if (placementIsBottom || placementIsTop) {
                    const { top, bottom } = element.getBoundingClientRect();
                    if (!pin || placementIsTop) {
                        setSpaceTop(top);
                    }
                    else if (pin) {
                        setSpaceTop(0);
                    }
                    if (!pin || placementIsBottom) {
                        setSpaceBottom(window.innerHeight - bottom);
                    }
                    else if (pin) {
                        setSpaceBottom(0);
                    }
                }
                else {
                    setSpaceTop(window.innerHeight);
                }
            }
        }
        if (isOpen) {
            window.addEventListener('resize', getVerticalSpace);
            getVerticalSpace();
        }
        return () => {
            window.removeEventListener('resize', getVerticalSpace);
        };
    }, [element, pin, placementIsBottom, placementIsTop, isOpen]);
    const max = Math.max(spaceTop, spaceBottom);
    const windowHeight = typeof window !== `undefined` ? window.innerHeight : 50;
    return max > 50 ? max : windowHeight;
}
function useOpenWithoutElement(isOpen, element) {
    const [openWithoutElem, setOpenWithoutElem] = useState(isOpen && element === null);
    useEffect(() => {
        if (element && openWithoutElem) {
            setOpenWithoutElem(false);
        }
    }, [openWithoutElem, element]);
    return openWithoutElem;
}
function isNodeInOrAfter(nodeA, nodeB) {
    const relationship = nodeA.compareDocumentPosition(nodeB);
    return (relationship === Node.DOCUMENT_POSITION_FOLLOWING ||
        relationship ===
            Node.DOCUMENT_POSITION_FOLLOWING + Node.DOCUMENT_POSITION_CONTAINED_BY);
}
function usePopoverToggle({ isOpen: controlledIsOpen = false, setOpen: controlledSetOpen, canClose, groupedPopoversRef, triggerToggle, }, portalElement, triggerElement) {
    const [uncontrolledIsOpen, uncontrolledSetOpen] = useState(controlledIsOpen);
    const [mouseDownTarget, setMouseDownTarget] = useState(null);
    const isControlled = useControlWarn({
        controllingProps: ['setOpen'],
        isControlledCheck: () => controlledSetOpen !== undefined,
        name: 'usePopover',
    });
    const isOpen = isControlled ? controlledIsOpen : uncontrolledIsOpen;
    const setOpen = isControlled && controlledSetOpen ? controlledSetOpen : uncontrolledSetOpen;
    useEffect(() => {
        function checkCloseAndStopEvent(event) {
            if (canClose && !canClose())
                return;
            if (portalElement && mouseDownTarget) {
                if (isNodeInOrAfter(portalElement, mouseDownTarget)) {
                    return;
                }
            }
            if (portalElement &&
                isNodeInOrAfter(portalElement, event.target)) {
                return;
            }
            const clickedOnToggle = triggerElement && triggerElement.contains(event.target);
            if (!triggerToggle && clickedOnToggle) {
                return;
            }
            setOpen(false);
            if (clickedOnToggle) {
                event.stopPropagation();
                return;
            }
            if (groupedPopoversRef &&
                groupedPopoversRef.current &&
                groupedPopoversRef.current.contains(event.target)) {
                return;
            }
            event.stopPropagation();
            event.preventDefault();
        }
        function handleMouseDown(event) {
            setMouseDownTarget(event.target);
            checkCloseAndStopEvent(event);
        }
        function handleClickOutside(event) {
            checkCloseAndStopEvent(event);
            setMouseDownTarget(null);
        }
        function handleMouseUp() {
            setMouseDownTarget(null);
        }
        if (isOpen) {
            document.addEventListener('mousedown', handleMouseDown, true);
            document.addEventListener('click', handleClickOutside, true);
        }
        else if (mouseDownTarget) {
            document.addEventListener('click', handleClickOutside, true);
            document.addEventListener('mouseup', handleMouseUp, true);
        }
        return () => {
            document.removeEventListener('mousedown', handleMouseDown, true);
            document.removeEventListener('click', handleClickOutside, true);
            document.removeEventListener('mouseup', handleMouseUp, true);
        };
    }, [
        canClose,
        groupedPopoversRef,
        isOpen,
        setOpen,
        triggerElement,
        portalElement,
        triggerToggle,
        mouseDownTarget,
    ]);
    return [isOpen, setOpen];
}
export function usePopover({ arrow = true, canClose, content, groupedPopoversRef, pin = false, isOpen: controlledIsOpen = false, onClose, placement: propsPlacement = 'bottom', setOpen: controlledSetOpen, triggerElement, triggerToggle = true, focusTrap = true, }) {
    const { element: scrollElement, callbackRef: scrollRef, enable: enableScrollLock, isEnabled: scrollLockEnabled, disable: disableScrollLock, } = useScrollLock(controlledIsOpen, true);
    const { callbackRef: focusRef, enable: enableFocusTrap, isEnabled: focusTrapEnabled, disable: disableFocusTrap, trapRef: focusTrapRef, } = useFocusTrap(controlledIsOpen && focusTrap);
    const { focusTrapRef: parentFocusTrapRef } = React.useContext(ModalContext);
    const [newTriggerElement, callbackRef] = useCallbackRef();
    const element = typeof triggerElement === 'undefined' ? newTriggerElement : triggerElement;
    const [isOpen, setOpen] = usePopoverToggle({
        canClose,
        groupedPopoversRef,
        isOpen: controlledIsOpen,
        setOpen: controlledSetOpen,
        triggerToggle,
    }, scrollElement, element);
    const verticalSpace = useVerticalSpace(element, pin, propsPlacement, isOpen);
    const openWithoutElem = useOpenWithoutElement(isOpen, element);
    useEffect(() => {
        if (isOpen) {
            if (focusTrap) {
                enableFocusTrap();
            }
            else {
                parentFocusTrapRef &&
                    parentFocusTrapRef.current &&
                    parentFocusTrapRef.current.deactivate({ returnFocus: false });
            }
        }
    }, [focusTrap, parentFocusTrapRef, isOpen, enableFocusTrap]);
    function handleOpen(event) {
        setOpen(true);
        enableScrollLock();
        event.stopPropagation();
        event.preventDefault();
    }
    function handleClose() {
        if (canClose && !canClose())
            return;
        setOpen(false);
        onClose && onClose();
    }
    const usePopperProps = useMemo(() => ({
        anchor: element,
        arrow,
        options: {
            modifiers: [
                {
                    enabled: !pin,
                    name: 'flip',
                    options: {
                        flipVariations: true,
                        flipVariationsByContent: true,
                    },
                },
                {
                    enabled: true,
                    name: 'eventListeners',
                    options: {
                        scroll: false,
                    },
                },
            ],
            placement: propsPlacement,
        },
    }), [arrow, element, pin, propsPlacement]);
    const { arrowProps, placement, popperInstanceRef, style, targetRef, } = usePopper(usePopperProps);
    const ref = useForkedRef(targetRef, focusRef);
    const [containerElement, contentContainerRef] = useCallbackRef();
    const popover = !openWithoutElem && isOpen && (React.createElement(ModalContext.Provider, { value: {
            closeModal: handleClose,
            disableFocusTrap,
            disableScrollLock,
            enableFocusTrap,
            enableScrollLock,
            focusTrapEnabled,
            focusTrapRef,
            scrollLockEnabled,
        } },
        React.createElement(ModalPortal, { ref: scrollRef },
            React.createElement(OverlaySurface, { arrow: arrow, arrowProps: arrowProps, placement: placement, ref: ref, style: style, backgroundColor: "palette.white", border: "1px solid", borderColor: "palette.charcoal200", borderRadius: "medium", boxShadow: 3, color: "palette.charcoal900" },
                React.createElement(Box, { maxHeight: `calc(${verticalSpace - 10}px - 1rem)`, overflowY: "auto", borderRadius: "inherit", ref: contentContainerRef }, content)))));
    return {
        contentContainer: containerElement,
        isOpen,
        open: handleOpen,
        popover,
        popperInstanceRef,
        ref: callbackRef,
    };
}
export function Popover(_a) {
    var { children, hoverDisclosureRef } = _a, props = __rest(_a, ["children", "hoverDisclosureRef"]);
    const { popover, open, ref, isOpen } = usePopover(props);
    const childrenOutput = children(open, ref, isOpen ? 'active' : '');
    const [isHovered] = useHovered(hoverDisclosureRef);
    const triggerShown = isHovered || isOpen;
    return (React.createElement(React.Fragment, null,
        popover,
        triggerShown && childrenOutput));
}
//# sourceMappingURL=Popover.js.map