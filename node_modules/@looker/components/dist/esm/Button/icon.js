import styled, { css } from 'styled-components';
import { rem } from 'polished';
import { Icon } from '../Icon';
export const iconMargins = (props) => {
    const spacing = { large: 0, small: 0 };
    switch (props.size) {
        case 'xsmall':
            spacing.small = 2;
            spacing.large = 4;
            break;
        case 'small':
        case 'large':
        default:
            spacing.small = 4;
            spacing.large = 8;
    }
    if (props.iconBefore) {
        return css `
      margin-left: -${rem(spacing.small)};
      margin-right: ${rem(spacing.large)};
    `;
    }
    else if (props.iconAfter) {
        return css `
      margin-left: ${rem(spacing.large)};
      margin-right: -${rem(spacing.small)};
    `;
    }
    else {
        return false;
    }
};
export const iconSizes = (props) => {
    let iconSize = 16;
    switch (props.size) {
        case 'xsmall':
            iconSize = 12;
            break;
        case 'small':
            iconSize = 14;
            break;
        case 'large':
            iconSize = 18;
            break;
        case 'medium':
        default:
            iconSize = 16;
    }
    return css `
    width: ${iconSize}px;
    height: ${iconSize}px;
  `;
};
export const ButtonIcon = styled(Icon).withConfig({ displayName: "ButtonIcon", componentId: "sc-19aiivs" }) ``;
export const buttonIcon = (props) => css `
  ${ButtonIcon} {
    ${iconMargins(props)};
    ${iconSizes(props)}
  }
`;
//# sourceMappingURL=icon.js.map