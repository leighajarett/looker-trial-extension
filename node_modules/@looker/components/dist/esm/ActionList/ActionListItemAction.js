import React, { useContext } from 'react';
import { MenuItem, MenuContext } from '../Menu';
export const ActionListItemAction = (props) => {
    const { setOpen } = useContext(MenuContext);
    const handleActionClick = (event) => {
        setOpen && setOpen(false);
        props.onClick && props.onClick(event);
    };
    return React.createElement(MenuItem, Object.assign({}, props, { onClick: handleActionClick }));
};
//# sourceMappingURL=ActionListItemAction.js.map