import React from 'react';
import styled from 'styled-components';
import { Checkbox } from '../Form';
export const checkListProps = ['checked', 'disabled', 'onChange'];
export const actionListCheckboxWidth = '2.75rem';
const ActionListCheckboxLayout = ({ onChange, checked, disabled, className, }) => {
    const handleOnChange = (event) => {
        const isNotFromChild = event.currentTarget === event.target;
        isNotFromChild && !disabled && onChange && onChange();
    };
    const handleOnKeyDown = (event) => {
        if (event.keyCode === 13) {
            event.currentTarget.click();
        }
    };
    return (React.createElement("div", { onClick: handleOnChange, className: className },
        React.createElement(Checkbox, { disabled: disabled, checked: checked, onChange: handleOnChange, onKeyDown: handleOnKeyDown })));
};
export const ActionListCheckbox = styled(ActionListCheckboxLayout).withConfig({ displayName: "ActionListCheckbox", componentId: "sc-3i5an4" }) `
  align-items: center;
  display: flex;
  justify-content: center;
  flex-basis: ${actionListCheckboxWidth};
  flex-shrink: 0;

  cursor: ${({ disabled }) => (disabled ? 'not-allowed' : 'default')};
`;
//# sourceMappingURL=ActionListCheckbox.js.map