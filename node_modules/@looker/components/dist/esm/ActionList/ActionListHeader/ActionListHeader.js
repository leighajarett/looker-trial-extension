import styled from 'styled-components';
import React, { useContext } from 'react';
import { ActionListRow } from '../ActionListRow';
import { ActionListContext } from '../ActionListContext';
const ActionListHeaderInternal = ({ children, className, }) => {
    const { allSelected, canSelect, onSelectAll } = useContext(ActionListContext);
    const hasCheckbox = typeof canSelect === 'boolean' ? canSelect : canSelect.all;
    return (React.createElement(ActionListRow, { className: className, hasCheckbox: hasCheckbox, onChange: onSelectAll, checked: allSelected }, children));
};
export const ActionListHeader = styled(ActionListHeaderInternal).withConfig({ displayName: "ActionListHeader", componentId: "sc-14g4l2h" }) `
  border-bottom: solid 1px ${(props) => props.theme.colors.palette.charcoal200};
  color: ${(props) => props.theme.colors.palette.charcoal900};
  display: flex;
  font-size: ${(props) => props.theme.fontSizes.xsmall};
  font-weight: ${(props) => props.theme.fontWeights.semiBold};
`;
//# sourceMappingURL=ActionListHeader.js.map