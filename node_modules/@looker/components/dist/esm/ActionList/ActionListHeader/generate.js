import React from 'react';
import { ActionListHeaderColumn } from './ActionListHeaderColumn';
import { ActionListHeader } from './ActionListHeader';
export const generateActionListHeaderColumns = (columns) => columns.map(({ id, title }) => (React.createElement(ActionListHeaderColumn, { key: id, id: id }, title)));
export const generateActionListHeader = (columns) => (React.createElement(ActionListHeader, null, generateActionListHeaderColumns(columns)));
//# sourceMappingURL=generate.js.map