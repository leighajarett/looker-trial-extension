import React from 'react';
import styled, { css } from 'styled-components';
import { Spinner } from '../Spinner';
import { NoResults } from './NoResults';
const ActionListManagerLayout = ({ children, className, isLoading, noResults, noResultsText, }) => {
    if (isLoading) {
        children = React.createElement(Spinner, null);
    }
    else if (noResults) {
        children = React.createElement(NoResults, null, noResultsText || 'No Results');
    }
    return React.createElement("div", { className: className }, children);
};
const centerItemStates = css `
  align-items: center;
  display: flex;
  justify-content: center;
  min-height: 8rem;
`;
export const ActionListManager = styled(ActionListManagerLayout).withConfig({ displayName: "ActionListManager", componentId: "sc-jhps37" }) `
  ${({ isLoading, noResults }) => (isLoading || noResults) && centerItemStates}
`;
//# sourceMappingURL=ActionListManager.js.map