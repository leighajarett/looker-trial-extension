import styled from 'styled-components';
import { defaultSpaceSize, spaceCSS } from './Space';
export const SpaceVertical = styled.div.withConfig({ displayName: "SpaceVertical", componentId: "sc-197c599" }) `
  ${spaceCSS}

  flex-direction: ${({ reverse }) => (reverse ? 'column-reverse' : 'column')};
  flex-shrink: 1;

  && > * {
    margin-top: ${({ theme, gap }) => theme.space[gap || defaultSpaceSize]};
  }

  ${({ theme, reverse }) => reverse
    ? `&& > :last-child { margin-top: ${theme.space.none}; }`
    : `&& > :first-child { margin-top: ${theme.space.none}; }`}
`;
SpaceVertical.defaultProps = { width: '100%' };
//# sourceMappingURL=SpaceVertical.js.map