import styled from 'styled-components';
import { defaultSpaceSize } from '../Space';
import { simpleLayoutCSS } from '../utils/simple';
export const Grid = styled.div.withConfig({ displayName: "Grid", componentId: "sc-e2j26h" }) `
  ${simpleLayoutCSS}

  display: grid;
  grid-gap: ${({ gap, theme }) => theme.space[gap || defaultSpaceSize]};
  grid-template-columns: ${({ columns }) => `repeat(${columns || 2}, minmax(0, 1fr))`};
`;
//# sourceMappingURL=Grid.js.map