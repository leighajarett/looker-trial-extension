import omit from 'lodash/omit';
import { createContext, } from 'react';
export const defaultData = {
    inputValue: undefined,
    navigationOption: undefined,
    option: undefined,
};
export const defaultMultiData = Object.assign(Object.assign({}, omit(defaultData, 'option')), { options: [] });
export const ComboboxContext = createContext({
    data: defaultData,
});
export const ComboboxMultiContext = createContext({
    data: defaultMultiData,
});
export const OptionContext = createContext(undefined);
//# sourceMappingURL=ComboboxContext.js.map