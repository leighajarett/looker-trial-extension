import { useContext, useLayoutEffect } from 'react';
export function useInputPropRefs({ autoComplete = true, readOnly = false }, context) {
    const { autoCompletePropRef, readOnlyPropRef } = useContext(context);
    useLayoutEffect(() => {
        if (autoCompletePropRef)
            autoCompletePropRef.current = autoComplete;
    }, [autoComplete]);
    useLayoutEffect(() => {
        if (readOnlyPropRef)
            readOnlyPropRef.current = readOnly;
    }, [readOnly]);
}
//# sourceMappingURL=useInputPropRefs.js.map