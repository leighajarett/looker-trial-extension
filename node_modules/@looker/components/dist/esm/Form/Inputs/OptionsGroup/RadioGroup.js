import React, { useState } from 'react';
import { Fieldset } from '../../Fieldset';
import { FieldRadio } from '../../Fields';
export const RadioGroup = ({ disabled, inline, options, defaultValue = '', value, onChange, }) => {
    const [currentOption, setCurrentOption] = useState(value || defaultValue);
    const handleOnchange = (option) => {
        !value && setCurrentOption(option);
        onChange && onChange(option);
    };
    const radios = options.map((option, index) => (React.createElement(FieldRadio, { checked: currentOption === option.value, disabled: option.disabled || disabled, key: index, label: option.label, onChange: () => handleOnchange(option.value) })));
    return (React.createElement(Fieldset, { "data-testid": "radio-list", disabled: disabled, inline: inline }, radios));
};
//# sourceMappingURL=RadioGroup.js.map