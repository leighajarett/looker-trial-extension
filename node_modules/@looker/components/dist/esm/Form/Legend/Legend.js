import styled from 'styled-components';
import { color, reset, space, typography, } from '@looker/design-tokens';
import { labelDefaults } from '../Label';
export const Legend = styled.legend.withConfig({ displayName: "Legend", componentId: "sc-on00wh" }) `
  ${reset}
  ${color};
  ${space};
  ${typography};
`;
Legend.defaultProps = Object.assign(Object.assign({}, labelDefaults), { fontSize: 'medium', padding: 'none' });
//# sourceMappingURL=Legend.js.map