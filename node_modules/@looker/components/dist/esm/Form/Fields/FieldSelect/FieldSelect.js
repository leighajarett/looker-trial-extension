import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { useID } from '../../../utils';
import { useFormContext } from '../../Form';
import { Select } from '../../Inputs/Select/Select';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
const FieldSelectComponent = forwardRef((props, ref) => {
    const validationMessage = useFormContext(props);
    const id = useID(props.id);
    return (React.createElement(Field, Object.assign({}, pickFieldProps(props), { id: id, validationMessage: validationMessage }),
        React.createElement(Select, Object.assign({}, omitFieldProps(props), { "aria-describedby": `${id}-describedby`, id: id, validationType: validationMessage && validationMessage.type, ref: ref }))));
});
FieldSelectComponent.displayName = 'FieldSelectComponent';
export const FieldSelect = styled(FieldSelectComponent).withConfig({ displayName: "FieldSelect", componentId: "sc-k32eks" }) ``;
//# sourceMappingURL=FieldSelect.js.map