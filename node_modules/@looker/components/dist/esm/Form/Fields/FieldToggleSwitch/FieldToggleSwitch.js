import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { useID } from '../../../utils';
import { useFormContext } from '../../Form';
import { ToggleSwitch, } from '../../Inputs/ToggleSwitch/ToggleSwitch';
import { omitFieldProps, pickFieldProps } from '../Field';
import { FieldInline } from '../FieldInline';
const FieldToggleSwitchLayout = forwardRef((props, ref) => {
    const validationMessage = useFormContext(props);
    const id = useID(props.id);
    return (React.createElement(FieldInline, Object.assign({}, pickFieldProps(props), { validationMessage: validationMessage, id: id }),
        React.createElement(ToggleSwitch, Object.assign({}, omitFieldProps(props), { "aria-describedby": `${id}-describedby`, id: id, validationType: validationMessage && validationMessage.type, ref: ref }))));
});
FieldToggleSwitchLayout.displayName = 'FieldToggleSwitchLayout';
export const FieldToggleSwitch = styled(FieldToggleSwitchLayout).withConfig({ displayName: "FieldToggleSwitch", componentId: "sc-11pxqwb" }) ``;
//# sourceMappingURL=FieldToggleSwitch.js.map