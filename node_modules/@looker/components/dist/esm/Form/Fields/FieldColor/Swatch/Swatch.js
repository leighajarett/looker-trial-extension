import omit from 'lodash/omit';
import styled from 'styled-components';
import { reset } from '@looker/design-tokens';
import { border, height, width, } from 'styled-system';
import { inputTextDefaults, inputTextDisabled, inputTextHover, } from '../../../Inputs/InputText';
export const Swatch = styled.div.withConfig({ displayName: "Swatch", componentId: "sc-w40q1v" }) `
  ${reset}
  ${width}
  ${height}
  ${border}

  &:hover {
    ${inputTextHover}
  }

  ${(props) => (props.disabled ? inputTextDisabled : '')}

  background-color: ${(props) => props.color};
  margin-top: auto;
  flex-shrink: 0;
`;
Swatch.defaultProps = Object.assign(Object.assign({}, omit(inputTextDefaults, 'fontSize')), { color: 'white', width: inputTextDefaults.height });
//# sourceMappingURL=Swatch.js.map