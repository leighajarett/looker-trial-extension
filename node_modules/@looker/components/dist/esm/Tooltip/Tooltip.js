var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useMemo, useState } from 'react';
import { ModalContext } from '../Modal';
import { useCallbackRef, usePopper, useForkedRef, } from '../utils';
import { OverlaySurface } from '../Overlay/OverlaySurface';
import { TooltipContent } from './TooltipContent';
export const CustomizableTooltipAttributes = {};
export function useTooltip({ arrow = true, canClose, content, isOpen: initializeOpen = false, width, textAlign, disabled, surfaceStyles, triggerElement, placement: propsPlacement = 'bottom', }) {
    const [isOpen, setIsOpen] = useState(initializeOpen);
    const [surfaceElement, surfaceCallbackRef] = useCallbackRef();
    const [newTriggerElement, callbackRef] = useCallbackRef();
    const element = typeof triggerElement === 'undefined' ? newTriggerElement : triggerElement;
    const handleOpen = () => setIsOpen(true);
    const handleClose = () => {
        if (canClose && !canClose())
            return;
        setIsOpen(false);
    };
    const handleMouseOut = (event) => {
        if (!isOpen)
            return;
        const related = event.relatedTarget;
        if (element && (element === related || element.contains(related))) {
            return;
        }
        if (surfaceElement &&
            (surfaceElement === related || surfaceElement.contains(related))) {
            return;
        }
        window.requestAnimationFrame(() => {
            handleClose();
        });
    };
    const eventHandlers = {
        onBlur: handleClose,
        onFocus: handleOpen,
        onMouseOut: handleMouseOut,
        onMouseOver: handleOpen,
    };
    const usePopperProps = useMemo(() => ({
        anchor: element,
        arrow,
        options: {
            modifiers: [
                {
                    enabled: true,
                    name: 'flip',
                    options: {
                        flipVariations: true,
                    },
                },
            ],
            placement: propsPlacement,
        },
    }), [arrow, element, propsPlacement]);
    const { arrowProps, placement, popperInstanceRef, style, targetRef, } = usePopper(usePopperProps);
    const ref = useForkedRef(targetRef, surfaceCallbackRef);
    const popper = isOpen && content && !disabled ? (React.createElement(ModalContext.Provider, { value: { closeModal: handleClose } },
        React.createElement(OverlaySurface, Object.assign({ arrow: arrow, arrowProps: arrowProps, eventHandlers: { onMouseOut: handleMouseOut }, placement: placement, ref: ref, style: style, zIndex: CustomizableTooltipAttributes.zIndex, backgroundColor: "palette.charcoal600", borderRadius: "medium", boxShadow: 3, color: "palette.charcoal000" }, surfaceStyles),
            React.createElement(TooltipContent, { width: width, textAlign: textAlign }, content)))) : null;
    return {
        eventHandlers,
        popperInstanceRef,
        ref: callbackRef,
        tooltip: popper,
    };
}
export const Tooltip = (_a) => {
    var { children } = _a, props = __rest(_a, ["children"]);
    const { eventHandlers, tooltip, ref } = useTooltip(props);
    return (React.createElement(React.Fragment, null,
        tooltip,
        children(eventHandlers, ref)));
};
//# sourceMappingURL=Tooltip.js.map