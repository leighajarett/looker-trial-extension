import React from 'react';
export const DateTimeFormat = ({ children = new Date(Date.now()), date, format, locale, time, timeZone, }) => {
    const options = {
        dateStyle: date ? format : undefined,
        timeStyle: time ? format : undefined,
        timeZone,
    };
    try {
        const formatted = children.toLocaleDateString(locale, options);
        return React.createElement(React.Fragment, null, formatted);
    }
    catch (error) {
        return React.createElement(React.Fragment, null, error);
    }
};
DateTimeFormat.defaultProps = {
    date: true,
    format: 'medium',
    time: true,
};
//# sourceMappingURL=DateTimeFormat.js.map