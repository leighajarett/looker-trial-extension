import React from 'react';
import { Paragraph } from '../../Text';
import { ModalContent, ModalFooter, ModalHeader } from '../Layout';
export const ConfirmLayout = ({ secondaryButton, primaryButton, message, title, titleIcon = undefined, }) => {
    return (React.createElement(React.Fragment, null,
        React.createElement(ModalHeader, { hideClose: true, headerIcon: titleIcon }, title),
        React.createElement(ModalContent, { innerProps: { py: 'none' } }, typeof message === 'string' ? (React.createElement(Paragraph, null, message)) : (message)),
        React.createElement(ModalFooter, null,
            primaryButton,
            secondaryButton)));
};
//# sourceMappingURL=ConfirmLayout.js.map