import React from 'react';
import styled from 'styled-components';
import { Select } from '../Form';
const defaultPageSizes = [10, 25, 50, 100];
const stringToSelectOption = (option) => ({
    label: String(option),
    value: String(option),
});
const arrayToSelectOptions = (options) => options.map((option) => stringToSelectOption(String(option)));
export const PageSizeLayout = ({ value, total, className, onChange, options = defaultPageSizes, }) => {
    const handleOnChange = (newValue) => onChange(Number(newValue));
    return Math.min(...options) < total ? (React.createElement("div", { className: className },
        "Display",
        React.createElement(Select, { width: "5rem", mx: "xsmall", options: arrayToSelectOptions(options), value: String(value), onChange: handleOnChange }),
        "of ",
        total)) : null;
};
export const PageSize = styled(PageSizeLayout).withConfig({ displayName: "PageSize", componentId: "sc-1v4lcjk" }) `
  align-items: center;
  display: flex;
  font-size: ${({ theme }) => theme.fontSizes.small};
`;
//# sourceMappingURL=PageSize.js.map