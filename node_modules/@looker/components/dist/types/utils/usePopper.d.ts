import { CSSProperties } from 'react';
import { Instance, Options } from '@popperjs/core';
import { ElementOrRef } from './getCurrentNode';
export interface UsePopperProps {
    anchor: ElementOrRef;
    target?: ElementOrRef;
    arrow?: boolean;
    options: Partial<Options> & {
        placement: Options['placement'];
    };
}
export declare function usePopper({ anchor, target, arrow, options, }: UsePopperProps): {
    arrowProps: {
        ref: (node: HTMLElement | null) => void;
        style: CSSProperties;
    };
    placement: import("@popperjs/core").Placement;
    popperInstanceRef: import("react").MutableRefObject<Instance | undefined>;
    style: CSSProperties;
    targetRef: (node: HTMLElement | null) => void;
};
