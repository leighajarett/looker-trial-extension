import { FC } from 'react';
import { Placement } from '@popperjs/core';
export interface MenuDisclosureProps {
    children: JSX.Element;
    tooltip?: string;
    tooltipPlacement?: Placement;
}
export declare const MenuDisclosure: FC<MenuDisclosureProps>;
