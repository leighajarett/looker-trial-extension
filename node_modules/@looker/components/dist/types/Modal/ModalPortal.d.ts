import { CustomizableAttributes } from '@looker/design-tokens';
import React from 'react';
export interface CustomizableModalAttributes extends CustomizableAttributes {
    zIndex?: number;
}
export declare const CustomizableModalAttributes: CustomizableModalAttributes;
export declare const ModalPortal: React.ForwardRefExoticComponent<{
    children: React.ReactNode;
} & React.RefAttributes<HTMLDivElement>>;
