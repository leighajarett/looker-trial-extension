import React from 'react';
import { ValidationType } from '../../ValidationMessage';
import { ComboboxProps } from '../Combobox';
import { SelectOptionsBaseProps } from './SelectOptions';
export interface SelectBaseProps extends SelectOptionsBaseProps {
    placeholder?: string;
    isClearable?: boolean;
    onFilter?: (term: string) => void;
    validationType?: ValidationType;
    windowedOptions?: boolean;
}
export interface SelectProps extends Omit<ComboboxProps, 'value' | 'defaultValue' | 'onChange'>, SelectBaseProps {
    value?: string;
    defaultValue?: string;
    onChange?: (value: string) => void;
}
export declare const Select: import("styled-components").StyledComponent<React.ForwardRefExoticComponent<SelectProps & React.RefAttributes<HTMLInputElement>>, any, {}, never>;
