import { FC } from 'react';
import { BorderProps, SpaceProps } from '@looker/design-tokens';
import { ValidationType } from '../../ValidationMessage';
import { TimeFormats } from '../utils';
declare type intervals = 5 | 10 | 15 | 30;
export interface InputTimeSelectProps extends SpaceProps, BorderProps {
    format?: TimeFormats;
    interval?: intervals;
    defaultValue?: string;
    value?: string;
    onChange?: (val?: string) => void;
    validationType?: ValidationType;
    onValidationFail?: (value: string) => void;
}
export declare const InputTimeSelect: FC<InputTimeSelectProps>;
export {};
