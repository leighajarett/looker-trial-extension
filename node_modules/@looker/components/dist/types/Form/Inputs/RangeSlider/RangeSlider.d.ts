import React, { FC } from 'react';
import { SpaceProps } from '@looker/design-tokens';
import { WidthProps } from 'styled-system';
import { ValidationType } from '../../ValidationMessage';
export interface RangeSliderProps extends SpaceProps, WidthProps {
    'aria-labelledby'?: string;
    max?: number;
    min?: number;
    step?: number;
    onChange?: (value: number[]) => void;
    value?: number[];
    defaultValue?: number[];
    disabled?: boolean;
    readOnly?: boolean;
    validationType?: ValidationType;
    className?: string;
}
export declare const InternalRangeSlider: FC<RangeSliderProps>;
export declare const RangeSlider: import("styled-components").StyledComponent<React.FC<RangeSliderProps>, any, {}, never>;
