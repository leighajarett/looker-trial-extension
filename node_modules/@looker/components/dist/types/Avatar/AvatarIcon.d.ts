import React from 'react';
import { IconNames } from '@looker/icons';
import { AvatarProps } from './Avatar';
export interface AvatarIconProps extends AvatarProps {
    icon?: IconNames;
    color?: string;
    bg?: string;
}
export declare const AvatarIcon: import("styled-components").StyledComponent<React.FC<AvatarIconProps>, any, {}, never>;
