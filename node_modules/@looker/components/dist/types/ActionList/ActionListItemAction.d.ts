import { CompatibleHTMLProps } from '@looker/design-tokens';
import { IconNames } from '@looker/icons';
import { ReactNode } from 'react';
export interface ActionListItemActionProps extends CompatibleHTMLProps<HTMLElement> {
    children?: ReactNode;
    detail?: ReactNode;
    icon?: IconNames;
    role?: 'button' | 'link';
}
export declare const ActionListItemAction: (props: ActionListItemActionProps) => JSX.Element;
